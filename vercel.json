{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "routes": [
    { "src": "/tasks", "dest": "/" },
    { "src": "/stats", "dest": "/" },
    { "src": "/[^.]+", "dest": "/", "status": 200 }
  ]
} 